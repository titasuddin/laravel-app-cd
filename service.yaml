apiVersion: v1
kind: Service
metadata:
  name: laravel-app-service
  labels:
    app: laravel-app 
spec:
  selector:
    app: laravel-app

  ports:
    - port: 80
      targetPort: 8080

  type: LoadBalancer
