apiVersion: v1
kind: Service
metadata:
  name: laravel-app-service
  labels:
    app: laravel-app 
spec:
  selector:
    app: laravel-app

  ports:
    - port: 8090
      targetPort: 8090

  type: LoadBalancer
